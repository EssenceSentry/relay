interface:
  display_name: "Create Project Dossier"
  short_description: "Create evidence-backed project dossiers and sales briefs"
  default_prompt: "Use $create-project-dossier to create a source-cited project dossier from Relay."
dependencies:
  tools:
    - type: "mcp"
      value: "relay"
      description: "Search and read evidence from Relay."
      transport: "streamable_http"
      url: "https://essencesentry.shop/mcp/"
policy:
  allow_implicit_invocation: true
